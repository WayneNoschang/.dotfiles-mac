# Environment variables for shell and core utilities.
# Zsh parameters: `man zshparam`

HELPDIR=$XDG_DATA_HOME/zsh/help
# PAGER='bat -p -l=man'

# cdpath+=($XDG_CONFIG_HOME) # Directories for quick accesss.
export SAVEHIST=1000

# WORDCHARS defines characters to be considered part of a word:
# WORDCHARS='*?_-.[]~=/&;!#$%^(){}<>' # Zsh default
# WORDCHARS= # Bash equivalent
